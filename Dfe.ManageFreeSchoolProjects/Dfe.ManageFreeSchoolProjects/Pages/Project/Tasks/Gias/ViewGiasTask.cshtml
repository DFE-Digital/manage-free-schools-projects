@page "/projects/{projectId}/tasks/gias"
@using Dfe.ManageFreeSchoolProjects.Constants;
@using Dfe.ManageFreeSchoolProjects.API.Contracts.Project.Tasks
@using Dfe.ManageFreeSchoolProjects.Pages.Project.Tasks.GIAS
@using DocumentFormat.OpenXml.Office2013.Drawing.Chart
@model Dfe.ManageFreeSchoolProjects.Pages.Project.Tasks.KickOffMeeting.ViewKickOffMeetingTask

@{
    ViewData["Title"] = $"Gias (Get information about schools) - {Model.Project.SchoolName}";

    var backlink = string.Format(RouteConstants.TaskList, Model.ProjectId);
    var editlink = string.Format(RouteConstants.EditKickOffMeeting, Model.ProjectId);
    var gias = Model.Project.Gias;
}

@section BeforeMain {
    <div role="navigation" class="govuk-width-container">
        <a href=@backlink class="govuk-back-link">Back</a>
    </div>
}

<div class="govuk-grid-row">
    <div class="govuk-grid-column-full">
        <h1 class="govuk-heading-xl" data-testid="title">
            <span class="govuk-caption-l" data-testid="school-name">
                @Model.Project.SchoolName
            </span>
            Kick-off meeting
        </h1>
        <govuk-summary-list>
            <govuk-summary-item label="Checked the Trust information" asp-for="@gias.CheckedTrustInformation" href="@editlink"/>
            <govuk-summary-item label="Application form Sent" asp-for="@gias.ApplicationFormSent" href="@editlink"/>
            <govuk-summary-item label="Application saved to Workspaces" asp-for="@gias.SavedToWorkspaces" href="@editlink"/>
            <govuk-summary-item label="URN sent to trust" asp-for="@gias.UrnSent" href="@editlink"/>
        </govuk-summary-list>
        <form method="post">
            <govuk-checkbox-input label="Mark this section as complete, you can still make changes later"
                                  asp-for="MarkAsCompleted"
                                  name="mark-as-completed"
                                  id="mark-as-completed" />
            <button class="govuk-button" data-module="govuk-button" data-testid="confirm">
                Confirm and continue
            </button>
        </form>
    </div>
</div>